#include <stdio.h>

int main() {
    int number, sum = 0;
    scanf("%d", &number);
    int temp = number;
    while (temp != 0) {
        sum += temp % 10;
        temp /= 10;
    }
    printf("Bird said:%d\n", number);
    printf("Alice must go in path-%d\n", sum);
    return 0;
}
