#include <stdio.h>

int main() {
    int number;
    scanf("%d", &number);

    if (number % 2 == 0) {
        printf("The given input %d is Even\n", number);
    } else {
        printf("The given input %d is Odd\n", number);
    }

    return 0;
}
